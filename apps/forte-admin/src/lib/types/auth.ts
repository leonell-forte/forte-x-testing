export type UserData = {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
  role: string;
};

export type LoginCopy = {
  image: { url: string };
  title: string;
  description: string;
};
